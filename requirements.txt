# Beacon Proxy 依赖
# 安装: pip install -r requirements.txt
#
# 依赖兼容：mitmproxy 9/10 要求 h11<0.15，httpcore 1.0.9 要求 h11>=0.16，二者冲突
# 解决：限制 httpcore<1.0.9，或升级 Python 3.12 后用 mitmproxy>=11
mitmproxy>=9.0
httpcore<1.0.9
pydantic>=2.0
typer>=0.9
pyyaml>=6.0
uvicorn>=0.24
fastapi>=0.104

# 可选: MCP 支持
# mcp>=1.0

# 可选: 大模型解析
# openai>=1.0
